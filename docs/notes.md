# Week 2 Lab Notes

## Feature: User Greeting (feat/user-greeting)

Added `greet()` and `farewell()` functions to src/app.js.
- `greet(name, course)` accepts a name and optional course parameter
- Throws an error if name is not a valid string
- `farewell(name)` returns a sign-off message
